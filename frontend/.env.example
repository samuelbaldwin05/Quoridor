VITE_ENVIRONMENT=development

# Always localhost — the browser is outside Docker
VITE_SUPABASE_URL=http://localhost:54321

# Publishable key — safe to expose. Run `bun x supabase start` or `bun x supabase status` to get it.
VITE_SUPABASE_PUBLISHABLE_KEY=

VITE_API_URL=http://localhost:8000
