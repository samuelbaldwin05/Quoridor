ENVIRONMENT=development

# In Docker: http://gateway (internal Docker network — the nginx gateway service)
# Locally:   http://localhost:54321
SUPABASE_URL=http://localhost:54321

# Deterministic for local Supabase — same for everyone. Run `supabase status` to confirm.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hj04zWl196z2-SBc0

# Deterministic for local Supabase — same for everyone.
SUPABASE_JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long

CORS_ORIGINS=["http://localhost:5173"]
