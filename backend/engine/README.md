# Backend Engine

Python port of the frontend TypeScript engine for server-side move validation in multiplayer games.

Must produce identical results to `frontend/src/engine/` for the same inputs. Any bug fix or rule change must be applied to both.
